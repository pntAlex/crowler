{
  "name": "crowler",
  "version": "1.0.0",
  "private": true,
  "description": "Crawler d'audit de liens, sans aucune dependance",
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --hot server.ts",
    "test": "bun test",
    "compile": "bun build --compile --minify --target=bun-linux-x64 server.ts --outfile crowler",
    "fixture": "bun fixture.ts"
  }
}
